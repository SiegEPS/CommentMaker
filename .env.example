# Canvas configuration (entered via UI, but can also be set here for dev)
# CANVAS_BASE_URL=https://your-school.instructure.com
# CANVAS_TOKEN=your_canvas_api_token

# Cookie encryption key (generate a random 32-char string)
SESSION_SECRET=change-me-to-a-random-string-32ch
